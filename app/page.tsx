import React from 'react'
import LandingPage from '@/app/(no-auth)/(landing-page)/beranda/page'

type Props = {}

const page = (props: Props) => {
  return (
    <LandingPage />
  ) 
}

export default page